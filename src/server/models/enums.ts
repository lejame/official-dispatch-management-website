export enum UserRole {
    ADMIN = 'admin',
    MODERATOR = 'moderator',
    USER = 'user'
}

export enum ContractType {
    FULLTIME = 'fulltime',
    PARTIME = 'partime'
}

export enum DocumentType {
    INCOMING = 'incoming',
    OUTGOING = 'outgoing',
    INTERNAL = 'internal'
}

export enum DocumentStatus {
    PENDING = 'pending',
    HANDLED = 'handled',
    APPROVED = 'approved',
    REJECTED = 'rejected'
}
